const transactionsList = {
    path: 'list'
}

const transaction = {
    path: 'transaction/:id'
}

const transactionAdd = {
    path: 'add'
}

export const TransactionsPaths = {
    transactionsList,
    transaction,
    transactionAdd
}